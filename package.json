{
    "name": "dtmoney-api",
    "version": "1.0.0",
    "scripts": {
        "start": "json-server --watch server.json --port $PORT"
    },
    "dependencies": {
        "json-server": "^0.17.3"
    }
}